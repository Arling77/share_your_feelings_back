spring:
  application:
    name: instant_message
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/share_your_feelings?characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
    initialization-mode: always
# mapper文件配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
